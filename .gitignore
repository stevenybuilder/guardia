# Secrets — never commit
.env
.env.*
!.env.example
*.local
secrets/
*.pem
*.key
*.p12
id_rsa*
credentials.json
service-account*.json

# Build working notes — local-only, may contain context/keys
buildlogs/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
*.iml
out/
.vscode/

# Build artifacts
build/
.gradle/
*.jar
*.zip
!gradle/wrapper/gradle-wrapper.jar

# Sandbox demo repos (cloned on first ./gradlew runIde)
demo-repos/

# Claude Code
.claude/settings.local.json

# IntelliJ Platform sandbox state (re-created on every ./gradlew runIde)
.intellijPlatform/
.kotlin/

# JVM crash dumps
hs_err_pid*.log
replay_pid*.log

